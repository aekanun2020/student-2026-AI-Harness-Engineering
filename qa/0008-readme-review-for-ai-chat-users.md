# Q&A Log — Entry 0008

**วันที่:** 2026-09-15
**Project:** student-2026-AI-Harness-Engineering

## คำถาม (verbatim)

ตรวจดูทุก readme แล้วบอกมาว่า ตรงไหนที่เสี่ยงต่อความไม่เข้าใจของผู้เรียนที่เป็นแค่ AI Chat User พร้อมเสนอข้อความแก้ไขหรือเพิ่มเติมให้เข้าใจ โดยไม่เปลี่ยนแปลงอะไร remote remote และ local repo

## คำตอบ (verbatim)

ตรวจครบทั้ง 7 ไฟล์แล้ว (root + Lab 1/2/3/3a/4/5/6) ไม่ได้แก้อะไรทั้ง local และ remote — ด้านล่างคือรายงาน โดยมอง "ผู้เรียนที่ใช้ ChatGPT/Claude เป็น แต่ไม่เคยเปิด terminal ไม่เคยเขียนโค้ด"

### A. ปัญหาระดับทั้ง repo (ควรแก้ที่ root README จุดเดียว เพราะ Lab 2/3 เป็นไฟล์ byte-identical แตะไม่ได้)

| # | ความเสี่ยง | อยู่ที่ไหน | ข้อเสนอ (ใส่ใน root README) |
|---|---|---|---|
| A1 | **ไม่มีที่ไหนบอกว่า terminal คืออะไร เปิดยังไง** — ผู้เรียนสาย chat จะติดตั้งแต่บรรทัดแรกของ "วิธีรัน" | ทุกไฟล์ | เพิ่มหัวข้อก่อน "วิธีรัน": *"ถ้าไม่เคยใช้ terminal: Mac กด ⌘+Space พิมพ์ Terminal / Windows เปิด PowerShell — ใน Lab นี้ใช้แค่ 3 คำสั่ง: `cd` (เข้าโฟลเดอร์), `python3` (รันโปรแกรม), `pip` (ติดตั้งไลบรารี) ทุกคำสั่งพิมพ์แล้วกด Enter"* |
| A2 | **ไม่มีขั้น "ติดตั้ง Python"** — Lab 1 เริ่มที่ `python3 -m venv` เลย ถ้าเครื่องไม่มี Python จะ error ตั้งแต่ก้าวแรก | Lab 1 ขั้น 2 | เพิ่มขั้น 0: *"ติดตั้ง Python 3.11+ จาก python.org แล้วเช็คด้วย `python3 --version`"* |
| A3 | **คำสั่งเก่าตกค้าง** `conda activate agentic-ai` และ `cd Python-Agent-LangGraph` — ทั้งสองอย่างไม่มีจริงใน repo นี้ (Lab 1 สอน venv, โฟลเดอร์ชื่อ student-2026-…) ผู้เรียนพิมพ์ตามจะ error ทั้งสองบรรทัด | Lab 2 บรรทัด 19, 26-27 · Lab 3 บรรทัด 20, 27-28 | root README เพิ่มกล่องเตือน: *"README ของ Lab 2 และ 3 เป็นสำเนาจาก repo ต้นทาง ถ้าเห็น `conda activate agentic-ai` ให้ใช้ `source .venv/bin/activate` แทน และถ้าเห็น `cd Python-Agent-LangGraph` ให้ใช้ `cd student-2026-AI-Harness-Engineering` แทน"* |
| A4 | **อ้างถึงสิ่งที่ไม่มีใน repo นี้** — Lab 8/LangGraph (Lab 3 บรรทัด 14), `screenshots/labs/lab3_agent_loop.png` (Lab 3 บรรทัด 97 — โฟลเดอร์ screenshots ไม่มีอยู่จริง) ผู้เรียนจะหาไม่เจอแล้วคิดว่าตัวเองพลาด | Lab 3 | root README เพิ่ม: *"Lab 7-9 และโฟลเดอร์ screenshots ที่ README บางไฟล์อ้างถึง อยู่ใน repo ต้นทางเท่านั้น ไม่มีในนี้ ข้ามได้"* |
| A5 | **ไม่มีใครบอกว่ารันแล้วเสียเงิน** — คนใช้ ChatGPT ชินกับเหมาจ่ายรายเดือน แต่ OpenRouter คิดตามการใช้ | root บรรทัด 24 | ต่อท้าย comment: *"API key = รหัสให้โปรแกรมเรียก AI แทนเรา (ห้ามแชร์) — OpenRouter ต้องสมัคร+เติมเงินก่อน ทุกครั้งที่รันมีค่าใช้จ่ายเล็กน้อย (`compare_models.py` เรียก 3 โมเดล แพงกว่าไฟล์อื่น)"* |
| A6 | **ไม่รู้ว่าข้อความในเครื่องหมายคำพูดคือ "คำถาม" ที่เปลี่ยนได้** — ผู้เรียนอาจคิดว่าเป็นส่วนหนึ่งของคำสั่งที่ต้องพิมพ์เป๊ะ | root บรรทัด 27-33 | เพิ่ม comment บรรทัดเดียวเหนือคำสั่ง: *"# ข้อความใน "..." = คำถามที่จะส่งให้ AI เปลี่ยนเป็นอะไรก็ได้"* |
| A7 | **ไม่มีคำอธิบายรูปแบบผลลัพธ์** `[step 1] THINK -> …` / `TOOL_USE` / `END_TURN` — ผู้เรียนเห็นแล้วไม่รู้ว่าบรรทัดไหนคือ AI คิด บรรทัดไหนคือคำตอบ | ทุก Lab ที่แสดง output | root README เพิ่มตารางสั้น: *"`[step N]` = รอบคิดที่ N · `THINK` = AI ตัดสินใจจะใช้ tool · `TOOL_USE` = โปรแกรมเรียก tool ให้แล้วได้ผลอะไร · `END_TURN` = AI ตอบสุดท้าย · `[answer]` = คำตอบที่คุณจะเห็นถ้าเป็นหน้าแชท"* |
| A8 | **ศัพท์ที่ไม่มีนิยามและโผล่ตั้งแต่บรรทัดแรก**: repo, byte-identical, process, RAM, deterministic, argument, token, context window, subprocess, SIGKILL | root ตาราง Lab list (บรรทัด 12-18) หนักที่สุด | เพิ่มหัวข้อ *"ศัพท์ที่ควรรู้ก่อนอ่าน (สำหรับคนที่เคยใช้แค่หน้าแชท)"* ตารางเดียว เช่น: **repo** = โฟลเดอร์โปรเจกต์บน GitHub · **system prompt** = คำสั่งตั้งต้นบอกบุคลิก/กติกา AI (เหมือน Custom Instructions) · **tool** = ปุ่มที่ AI กดเองได้ เช่น เครื่องคิดเลข (เหมือน ChatGPT "กำลังค้นเว็บ…") · **agent loop** = AI ทำหลายขั้นต่อเนื่องจนเสร็จ ไม่ใช่ตอบทีเดียวจบ · **token** = ชิ้นส่วนคำ ค่าใช้จ่ายและขีดจำกัดคิดเป็น token · **process** = โปรแกรมที่รันอยู่ 1 ครั้ง ปิดแล้วความจำใน RAM หาย · **deterministic** = ทำเหมือนเดิม 100% ทุกครั้ง ไม่ขึ้นกับการสุ่มของ AI · **argument** = ค่าที่ส่งให้ tool (ไม่ใช่การเถียง) · **byte-identical** = ไฟล์เหมือนต้นฉบับทุกตัวอักษร |
| A9 | ตาราง 8 Layer + ตาราง coverage (root บรรทัด 40-117) เป็นเนื้อหาระดับผู้สอน/ผู้ตรวจ ผู้เรียนสาย chat อ่านแล้วท้อ | root | เพิ่มบรรทัดเดียวก่อนหัวข้อ: *"ส่วนนี้สำหรับผู้สอน/ผู้ตรวจ — ผู้เรียนข้ามไปอ่าน README ของแต่ละ Lab ได้เลย"* |

### B. ปัญหาเฉพาะไฟล์

#### Lab 1 (แก้ได้ ไม่ติด byte-identical)
| บรรทัด | ความเสี่ยง | ข้อเสนอ |
|---|---|---|
| ขั้น 2 (`source .venv/bin/activate`) | ไม่บอกว่า venv คืออะไร และ **ต้องรันซ้ำทุกครั้งที่เปิด terminal ใหม่** — จุดที่มือใหม่ติดบ่อยที่สุด (รันได้เมื่อวาน วันนี้ error) | เพิ่ม: *"venv = กล่องแยกสำหรับติดตั้งไลบรารีของโปรเจกต์นี้โดยเฉพาะ — **ทุกครั้งที่เปิด terminal ใหม่ต้องพิมพ์บรรทัดนี้ก่อน** ถ้าเห็น `(.venv)` หน้า prompt แปลว่าพร้อม"* |
| ขั้น 4 (แก้ไฟล์ `.env`) | ไฟล์ขึ้นต้นด้วยจุด **ถูกซ่อนใน Finder/Explorer** ผู้เรียนหาไม่เจอ | เพิ่ม: *"ไฟล์ที่ขึ้นต้นด้วย `.` จะมองไม่เห็นใน Finder/Explorer — เปิดจาก terminal ด้วย `open -e .env` (Mac) หรือ `notepad .env` (Windows)"* |
| "return code 0/1" | ไม่มีความหมายสำหรับคนไม่เขียนโค้ด | แทนด้วย: *"เห็น ✅ = พร้อม, เห็น ⚠️ = ยังไม่พร้อม แค่นั้น"* |
| "thin client", "OpenAI SDK + base_url" | ศัพท์ | เพิ่ม: *"เราใช้ไลบรารีของ OpenAI แต่ชี้ไปที่ OpenRouter ซึ่งเป็นตัวกลางที่ให้เลือกโมเดลจากหลายบริษัทได้ด้วยคีย์เดียว"* |

#### Lab 2 (byte-identical — ปัญหา A3 ทั้งหมดอยู่ที่นี่ด้วย)
| บรรทัด | ความเสี่ยง | ข้อเสนอ |
|---|---|---|
| 38-88 หัวข้อ "`llm` กับ `config` มาจากไหน" | เขียนดีสำหรับคนเคยเขียนโค้ด แต่ "module / instance ของคลาส / sys.path / แพ็กเกจ" จะทำให้ผู้เรียนสาย chat คิดว่าต้องเข้าใจถึงจะไปต่อได้ | ใส่ใน QUESTIONS.md ของ Lab 2 (ไฟล์ใหม่ ไม่ติด constraint) : *"ส่วน 'ใต้ฝา' ใน README ข้ามได้ถ้ายังไม่เขียน Python — ไม่กระทบการรัน"* |
| 12, 109 "token usage", "context window" | ศัพท์ (ครอบคลุมด้วย A8) | — |

#### Lab 3 (byte-identical — แก้ที่ QUESTIONS.md หรือ root เท่านั้น)
| บรรทัด | ความเสี่ยง | ข้อเสนอ |
|---|---|---|
| 11-13 "while loop", "OpenAI function schema", "JSON Schema", "list ของ dict" | หนักมากสำหรับ Lab แรกที่มี agent — แต่**ไม่มี analogy กับสิ่งที่ผู้เรียนเคยเห็นในหน้าแชทเลย** | ใส่ใน Lab 3 QUESTIONS.md ด้านบนสุด: *"เคยเห็น ChatGPT ขึ้นว่า 'กำลังค้นหาเว็บ…' แล้วค่อยตอบไหม? นั่นคือ AI ตัดสินใจใช้ tool → ดูผล → ตอบ — Lab นี้เราเขียน 'วง' นั้นเองด้วยมือ แต่ tool คือเครื่องคิดเลขกับนาฬิกาแทนการค้นเว็บ"* |
| 14, 97 | อ้าง Lab 8 และ screenshot ที่ไม่มี (A4) | — |

#### Lab 3a
| บรรทัด | ความเสี่ยง | ข้อเสนอ |
|---|---|---|
| 16 "แก้ไข argument" | "argument" = การเถียง ในความเข้าใจทั่วไป | *"argument (ค่าที่ส่งให้ tool — ในที่นี้คือข้อความสูตรเลข)"* |
| 68 "deterministic" | ศัพท์ (A8) | — |
| 22-30 | ผู้เรียนสาย chat **รู้จัก system prompt อยู่แล้ว** แต่ README ไม่เชื่อมให้ | เพิ่มประโยคเดียว: *"SYSTEM ตรงนี้คือสิ่งเดียวกับ Custom Instructions / system prompt ที่คุณเคยตั้งในหน้าแชท — Lab นี้แค่เพิ่มกติกาข้อเดียวเข้าไป"* |

#### Lab 4 (หนักที่สุดในบรรดาทั้งหมด)
| บรรทัด | ความเสี่ยง | ข้อเสนอ |
|---|---|---|
| ทั้งไฟล์ | อธิบาย**กลไก** (HookResult/HookManager) ดีมาก แต่**ไม่มีประโยคเดียวที่บอกว่า hook คืออะไรในภาษาคน** ก่อนลงรายละเอียด | เพิ่มใต้หัวเรื่อง: *"hook = ผู้ช่วยที่ยืนข้าง AI คอยตรวจ**ทุกครั้ง**ก่อน AI จะกดปุ่มอะไร ถ้าผิดกติกาก็ห้ามกด — ต่างจาก Lab 3a ที่ 'ขอร้อง' AI ผ่าน prompt ตรงนี้ 'บังคับ' ด้วยโปรแกรม AI ไม่มีสิทธิ์เลือก"* |
| 33 ตารางรีเสิร์ช "subprocess/HTTP คุยกันผ่าน JSON บน stdin/stdout" | **คุณเองก็สับสนจุดนี้มาแล้ว** ในบทสนทนานี้ (คิดว่า hook ของเราใช้ subprocess) — ผู้เรียนจะสับสนแน่ | เพิ่มบรรทัดใต้ตาราง: *"หมายเหตุ: ตารางนี้อธิบาย**ของจริง**ของ Claude Code/OpenAI เท่านั้น — hook ใน repo นี้ (`core/hooks.py`) เป็น Python function ธรรมดา **ไม่ใช้ subprocess/HTTP/JSON เลย**"* |
| 27-42 | "Claude Code", "OpenAI Agents SDK" ไม่รู้ว่าคืออะไร | *"Claude Code = โปรแกรม AI เขียนโค้ดของ Anthropic · Agents SDK = ชุดเครื่องมือสร้าง agent ของ OpenAI — ทั้งคู่มีกลไก 'ดักจังหวะ' นี้ในตัว เราเอาแนวคิดมาเขียนเองแบบง่าย"* |
| 59-66 โค้ด `@dataclass` / `str \| None` | อ่าน syntax ไม่ออกแล้วหยุด ทั้งที่ตารางบรรทัด 70-75 อธิบายครบแล้ว | เพิ่มก่อนโค้ด: *"ไม่ต้องอ่าน syntax ออก — 4 บรรทัดในกล่องคือ 'ช่อง' 4 ช่องของแบบฟอร์ม ดูตารางถัดไปพอ"* |
| 126 "audit trail", 189 "agent_audit.log" | ไม่รู้ว่าคืออะไร/เปิดยังไง | *"audit trail = สมุดบันทึกว่า AI ทำอะไร ตอนไหน เอาไว้ตรวจย้อนหลัง — ไฟล์ .log เปิดด้วย text editor ธรรมดา 1 บรรทัด = 1 เหตุการณ์"* |
| 199 "monkeypatch", 23 "stub" | ศัพท์ | *"monkeypatch/stub = สลับตัวเรียก AI จริงเป็นตัวปลอมที่ตอบตามสคริปต์ เพื่อทดสอบได้โดยไม่เสียเงิน/ไม่ต้องมีคีย์"* |

#### Lab 5
| บรรทัด | ความเสี่ยง | ข้อเสนอ |
|---|---|---|
| **20** | **บั๊กจริง 2 จุดในบรรทัดเดียว:** (1) "(มด/step)" เป็น typo ไม่มีความหมาย (2) บอกว่า checkpoint "ทำหน้าที่เดียวกับใน Lab 6 และ Lab 3/3a/4" — **Lab 6 ไม่มี checkpoint แล้ว** (ย้ายมาที่นี่ตอนแยก lab) และ Lab 3/3a/4 ก็ไม่เคยมี — เป็นข้อความตกค้างจากตอน Lab 5 ยังเป็น sandbox+checkpoint | เขียนใหม่ทั้ง bullet: *"- เห็น **checkpoint** — บันทึกสถานะลงไฟล์เป็นระยะ เพื่อให้ปิดโปรแกรมแล้วเปิดใหม่ก็คุยต่อได้ และถ้าโปรแกรมล่มกลางทางก็กลับมาทำต่อจากจุดที่ค้าง"* |
| **72** | "(เหมือนที่ทดสอบใน Lab 6 แต่ประยุกต์กับ memory)" — ตกค้างเหมือนกัน Lab 6 ไม่มี SIGKILL test แล้ว | ตัดวงเล็บออก |
| 15-19 | ไม่เชื่อมกับ **memory feature ของ ChatGPT/Claude ที่ผู้เรียนเคยใช้** | เพิ่ม: *"เคยสังเกตไหมว่า ChatGPT จำได้ว่าคุณเคยบอกอะไรไว้เมื่อวาน? Lab นี้สร้างสิ่งนั้นเองให้เห็นว่าข้างในทำยังไง — และ compaction คือเหตุผลที่แชทยาวๆ AI จะเริ่ม 'ลืม' ตอนต้น (มันสรุปทิ้งเพื่อประหยัดที่)"* |
| 74 "SIGKILL" | ศัพท์ | *"SIGKILL = ระบบปฏิบัติการฆ่าโปรแกรมทันทีแบบไม่มีโอกาสเซฟ (เหมือนดึงปลั๊ก)"* |
| 103 `[thread_id]` | ไม่รู้ว่า `[ ]` = ใส่หรือไม่ก็ได้ และ thread_id คือชื่อที่**ตั้งเอง** | *"thread_id = ชื่อบทสนทนาที่คุณตั้งเอง (เช่น my-thread) ใช้ชื่อเดิม = คุยต่อ ใช้ชื่อใหม่ = เริ่มใหม่ — `[ ]` แปลว่าใส่หรือไม่ใส่ก็ได้"* |
| 108 "gitignored" | ศัพท์ | ตัดทิ้งหรือ *"(ไม่ถูกอัปโหลดขึ้น GitHub)"* |

#### Lab 6
| บรรทัด | ความเสี่ยง | ข้อเสนอ |
|---|---|---|
| 15-16 "eval()", "resource exhaustion" | ไม่รู้ว่า eval คืออะไร ทำไมอันตราย | *"eval() = สั่งให้ Python คำนวณสูตรจากข้อความ เช่น '15*4' — อันตรายเพราะถ้าสูตรใหญ่มาก (เลขยกกำลังมหาศาล) เครื่องอาจค้าง"* + analogy: *"sandbox = ห้องแล็บที่มีผนังกันระเบิด — ทดลองพลาด ห้องพัง แต่ตึกไม่พัง"* |
| 38-51 โค้ด | อ่านไม่ออก | เพิ่มก่อนโค้ด: *"สาระของโค้ดนี้: แทนที่จะคำนวณในโปรแกรมหลัก เราเปิด 'โปรแกรมลูก' แยกออกมาคำนวณ ตั้งเวลาไว้ เกินเวลาก็ฆ่าโปรแกรมลูกทิ้ง โปรแกรมหลักไม่เป็นไร"* |
| 57-67 RLIMIT_AS / Darwin kernel / ValueError | เป็นบันทึกสำหรับผู้สอน ผู้เรียนสาย chat จะงงหนัก | เพิ่ม TL;DR บรรทัดแรก: *"สรุปสั้น: บน Mac จำกัด memory ไม่ได้ จำกัดได้แค่เวลา CPU — โค้ดเลยเขียนให้ทำงานได้ทั้ง Mac และ Linux ส่วนรายละเอียดข้างล่างข้ามได้"* |
| 78 "exit code -24 = SIGXCPU" | ศัพท์ | *"ตัวเลขติดลบ = โปรแกรมลูกถูกระบบฆ่า ไม่ใช่จบเอง"* |
| 94-99 ตาราง Docker/E2B/gVisor | ชื่อที่ไม่รู้จักทั้งหมด | เพิ่มบรรทัดก่อนตาราง: *"ไม่ต้องรู้จักทุกตัว แค่รู้ว่าของจริงในอุตสาหกรรมกันหนากว่าที่เราทำใน Lab นี้มาก"* |

### สรุปลำดับความสำคัญ

1. **แก้ก่อน (บั๊กจริง ไม่ใช่แค่เข้าใจยาก):** Lab 5 บรรทัด 20 และ 72 ข้อความตกค้าง/typo · A3 คำสั่ง conda ที่รันไม่ได้ · A4 อ้างของที่ไม่มี
2. **กันผู้เรียนติดตั้งแต่ก้าวแรก:** A1 terminal · A2 ติดตั้ง Python · Lab 1 venv ต้องรันซ้ำ + ไฟล์ .env ซ่อน · A5 เรื่องเงิน
3. **ทำให้อ่านต่อได้:** A8 ตารางศัพท์ · A7 อธิบายรูปแบบ output · analogy หน้าแชทใน Lab 3/4/5 · Lab 4 disclaimer เรื่อง subprocess

ทั้งหมดนี้ยังไม่ได้ลงมือแก้ตามที่สั่งครับ — ถ้าจะให้ลงมือ บอกได้ว่าเอาข้อไหน (โดยเฉพาะ A3/A4 ที่ต้องแก้ที่ root README แทนเพราะ Lab 2/3 แตะไม่ได้) หรือถ้าอยากได้รายงานนี้เป็นหน้าเว็บสำหรับส่งต่อให้ผู้สอนคนอื่น ผมทำเป็น artifact ให้ได้
